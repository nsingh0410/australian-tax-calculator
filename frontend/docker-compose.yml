version: '3.8'

services:
  frontend:
    build: .
    container_name: tax_calculator_frontend
    ports:
      - "3000:80"
    environment:
      - REACT_APP_GRAPHQL_URL=http://localhost:4000/graphql
    restart: unless-stopped